events {}

http {
    upstream apis {
        server api-1:5000;
        server api-2:5000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://apis;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $remote_addr;
        }
    }
}
