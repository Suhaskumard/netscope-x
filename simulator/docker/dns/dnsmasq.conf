# Minimal dnsmasq config for the lab. no-resolv/no-hosts keep this
# self-contained (no dependency on outbound internet DNS during
# verification) -- it only needs to answer for its own static record.
no-resolv
no-hosts
address=/example.lab/10.10.10.10
log-queries
