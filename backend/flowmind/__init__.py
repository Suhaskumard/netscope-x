"""FLOWMIND: behavioral intelligence over reconstructed flows (spec Phases 33-42).

Behavioral feature computation (`features/`), fingerprints, role
classification, baseline, and anomaly detection, per the master spec's
target architecture (TOPOLOGY INFERENCE -> FLOWMIND -> Behavioral/Anomaly).
Only `features/` (Phase 33) is implemented so far.
"""
