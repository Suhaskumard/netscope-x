"""Topology inference (spec Phases 29-32): node discovery, edge discovery,
probabilistic edge confidence, and the combined probabilistic topology graph.
Only node discovery (Phase 29) is implemented so far."""
