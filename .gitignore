.venv/
__pycache__/
*.pyc
.pytest_cache/
frontend/node_modules/
frontend/dist/

# Generated research artifacts (Phase 10/16+) -- reproducible from code, not source.
experiments_data/

# Real/secret environment configuration -- never commit these.
# .env.example and .env.test are intentionally NOT ignored (no secrets in them).
.env
.env.production
.env.local
.env.*.local
