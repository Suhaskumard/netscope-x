[project]
name = "netscope-client"
version = "0.1.0"
description = "Python client for the NETSCOPE-X REST API (/api/v1)"
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["netscope_client"]
