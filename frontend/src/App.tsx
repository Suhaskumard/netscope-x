/**
 * Phase 06 placeholder. Confirms the React + TypeScript + Vite + Tailwind
 * toolchain builds end-to-end. The real frontend areas (Overview, Topology
 * Explorer, Node Investigation, Timeline, ...) are built starting Phase 12
 * against the real API from Phase 09 -- this is not that.
 */
export default function App(): JSX.Element {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-950 text-slate-100">
      <div className="text-center">
        <h1 className="text-2xl font-semibold tracking-tight">NETSCOPE-X</h1>
        <p className="mt-2 text-sm text-slate-400">
          Development environment scaffold (Phase 06). No product UI yet.
        </p>
      </div>
    </main>
  );
}
